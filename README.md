# AI Toolbox Suite

AI Toolbox Suite is a collection of desktop productivity tools written in Python.

## Modules

- Image Toolbox
- PDF Toolbox (in development)

The repository is organized so that each tool lives in its own module under the `modules/` directory.
